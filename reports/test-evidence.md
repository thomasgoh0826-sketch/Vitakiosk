# VitaKiosk Test Evidence

Evidence date: 2026-06-28

| Evidence | Command | Actual result | Status |
|---|---|---|---|
| Solid interactive VitaKiosk mini app | `npm.cmd run site:test`; `npm.cmd run site:build`; in-app Browser QA at `http://127.0.0.1:5176/` and `/vitakiosk`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`. | Site suite passed with 1 file and 20 tests. Site production build passed. Browser QA confirmed `InteractiveVitaKioskMiniApp` renders real component UI with 10 buttons, zero `img` screenshot bases, zero `iframe` elements, and zero `.demo-hotspot` controls. Tap to Speak showed the simulated answer and highlighted product/shelf/promotion; fuzzy `Relief Bomb` opened `Do you mean Relief Balm?`, candidate selection returned the answer state; product enlarged and toggled to detail; promotion, shelf route, scan result, pharmacist handoff, and BM language state all worked without backend, mic, camera, localhost iframe, or local demo dependency. Compact journey fallback now restores visible scenes when ScrollTrigger is disabled, so the homepage demo is no longer hidden at tablet/mobile widths. Screenshots were emitted in the browser session and were not committed. | Pass |
| Lightweight glow and ripple backdrop | `npm.cmd run site:test`; `npm.cmd run site:build`; In-app Browser inspection at `http://127.0.0.1:5176/`; Playwright Chromium rendered QA for pointer movement, click splash, ripple cap, settle cleanup, and reduced-motion mode; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`. | Site suite passed with 1 file and 20 tests. Site production build passed. In-app Browser confirmed the global backdrop is a fixed `DIV` with `data-effect="pointer-glow-ripple"`, `pointer-events: none`, `filter: none`, `backdrop-filter: none`, `data-ripple-throttle-ms="150"`, `data-max-ripples="16"`, and no old liquid canvas. The in-app coordinate mover hit a browser-control coordinate issue, so rendered interaction QA used Playwright. Playwright confirmed movement/click produced 9 active ripples, including 7 move ripples and 2 splash ripples; rapid clicks stayed capped at 16; ripples self-removed back to 0 after settle; cursor CSS variables updated; reduced-motion mode set `data-reduced-motion="true"` and spawned 0 ripples; console errors: 0. Repository contract, 13 spec checks, staged-file safety, and provider secret scan passed. Temporary screenshot `C:\Users\Admin\AppData\Local\Temp\codex-vitakiosk-site-qa\lightweight-backdrop-active.png` was not committed. | Pass |
| Glow backdrop and smooth media orbit controls | `npm.cmd run site:test`; `npm.cmd run site:build`; Playwright Chromium rendered QA at `http://127.0.0.1:5176/` and `#video`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`. | Site suite passed with 1 file and 19 tests. Site production build passed. Browser plugin selection/reload timed out, so Playwright fallback was used. Rendered QA confirmed the global backdrop is a fixed `DIV` with `data-effect="pointer-glow"`, `pointer-events: none`, and no old liquid canvas or `liquid-wake` effect. Pointer movement changed glow variables from `50.00%/42.00%` to about `68.16%/63.40%`. Idle orbit progress advanced continuously with deltas `0.042-0.052` across seven samples, hover pause delta was `0`, pointer leave resumed by `0.119`, wheel inside the orbit rotated by `0.827` while page scroll stayed `0`, wheel outside the orbit scrolled the page by `540`, drag resumed by `0.282`, arrows were absent, visible card overlap was `0`, hover mounted one muted inline preview video, and click opened/Escape closed the viewer. Temporary screenshots were saved under `%TEMP%\\codex-vitakiosk-site-qa\\` and were not committed. | Pass |
| Spacious continuous media orbit correction | `npm.cmd run site:test`; `npm.cmd run site:build`; `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-provider-secrets.mjs`; Playwright Chromium visual/touch QA at `http://127.0.0.1:5176/#video`. | Site suite passed with 1 file and 19 tests, including the guard that the media orbit renders exactly 7 logical cards instead of a 3-copy visible buffer. Site production build passed. Backend suite passed with 153 tests. Existing kiosk frontend suite passed with 22 files and 188 tests, and production build passed. Repository contract, 13 spec checks, and provider secret scan passed. Browser QA confirmed idle auto-rotation advanced `0.155`, hover pause delta was `0`, mouse leave resumed by `0.141`, drag plus pointer leave resumed after delay by `0.063` then continued by `0.040`, touch tap changed progress by only `0.005` and resumed at `0.110`, no previous/next arrows rendered, the visible card count stayed spacious at 4, the active card had zero overlap with non-active visible cards, preview mounted one muted inline video, and click opened/Escape closed the viewer. Temporary screenshots under `tmp/site-qa/` were not committed. | Pass |
| Global pointer liquid wake repair | `npm.cmd run site:test`; `npm.cmd run site:build`; Playwright Chromium visual and canvas-energy QA at `http://127.0.0.1:5176/` for desktop pointer movement, mobile touch movement, settle decay, and reduced-motion mode. | Site suite passed with 1 file and 19 tests. Site production build passed. Browser QA confirmed `GlobalLiquidBackdrop` is fixed, global, and `pointer-events: none`; the canvas reports `data-effect="liquid-wake"`; desktop pointer movement increased canvas energy from `9105944` to `10180553` and then settled to `9917419`; mobile touch movement increased energy from `8052245` to `10924089`; reduced-motion context reported `data-reduced-motion="true"`. Visual screenshots under `tmp/site-qa/` showed a soft directional cyan/violet wake instead of concentric pointer rings and were not committed. | Pass |
| Embedded interactive VitaKiosk website demo | `npm.cmd run site:test`; `npm.cmd run site:build`; Playwright Chromium visual QA at `http://127.0.0.1:5176/vitakiosk`; `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`. | Site suite passed with 1 file and 19 tests, including screenshot base, no iframe/localhost dependency, Tap to Speak, fuzzy match, product enlarge/toggle, promotion viewer, shelf route, scan simulation, pharmacist handoff, and language labels. Site production build passed. Browser QA confirmed the embedded demo uses `/assets/reference/vitakiosk-demo-approved.png`, applies no blur to the screenshot base, renders zero iframe elements, keeps the local live link as `demo-dev-link`, and supports Tap to Speak, fuzzy candidate selection, product summary/detail morph, promotion viewer, shelf route close, scan candidate selection, pharmacist handoff close, and BM/EN language label changes directly inside the website. Temporary screenshots were saved under `tmp/site-qa/` and were not committed. Backend suite passed with 153 tests. Existing kiosk frontend passed with 22 files and 188 tests, and production build passed. Repository contract, 13 spec checks, staged-file safety, and provider secret scan passed. | Pass |
| Continuous media orbit repair | `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `npm.cmd run site:test`; `npm.cmd run site:build`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`; Playwright Chromium visual QA at `http://127.0.0.1:5176/#video`. | Backend suite passed with 153 tests. Existing kiosk frontend passed with 22 files and 188 tests, and production build passed. Site suite passed with 1 file and 14 tests, and production build passed. Repository contract, 13 spec checks, staged-file safety, and provider secret scan passed. Browser QA measured a true circular render buffer with 21 orbit cards for 7 logical films, minimum 10 visible cards, all 7 logical films represented while rotating, zero previous/next arrows, no bad active samples, and no empty center samples. Idle progress advanced `0.324`; hover pause delta was `0`; mouse leave resumed by `0.220`; touch hold stayed paused with delta `0.001`; touch end resumed by `0.415`; drag end resumed by `0.132`; modal close resumed by `0.197`. Hover preview mounted one muted inline video and played it (`paused=false`, `readyState=4`); click opened the viewer and Escape closed it. Temporary screenshots were saved under `tmp/site-qa/` and were not committed. | Pass |
| VitaKiosk Asia immersive motion and interactivity repair | `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `npm.cmd run site:test`; `npm.cmd run site:build`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`; Playwright via local Chrome at `http://127.0.0.1:5176/`, `/vitakiosk`, and `/vitaflow`; visual inspection of approved reference and rendered screenshots. | Backend suite passed with 153 tests. Existing kiosk frontend passed with 22 files and 188 tests, and production build passed. Site suite passed with 1 file and 14 tests. Site production build passed. Repository contract, 13 spec checks, staged-file safety, and provider secret scan passed. Browser QA reported no console errors; `GlobalLiquidBackdrop` existed once per page, was fixed with `pointer-events: none`, and pointer CSS variables changed from `0.5/0.42` to `0.0833/0.1778` and `0.9097/0.9222` from movement outside the demo; scroll down/up active scene indexes were stable; spherical video orbit auto-rotated from VitaKiosk Interactive Demo to VitaFlow Source of Truth, paused on hover, resumed after leaving, changed by drag, had no arrows or horizontal scrollbar, mounted/played one muted inline preview, and opened/closed the viewer; interactive VitaKiosk demo used the approved screenshot at opacity `1` with `object-fit: contain`, rendered zero fake avatar/center/right rail panels, and verified Tap to Speak, fuzzy match, product, promotion, screenshot-overlaid shelf route, scan, and assistance states; `/vitaflow` had no `Route-ready page` placeholder; desktop/mobile had zero horizontal overflow. Temporary screenshots were saved under `tmp/site-qa/` and were not committed. | Pass |
| VitaKiosk Asia flagship website framework | `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `npm.cmd run site:test`; `npm.cmd run site:build`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`; Playwright Chromium visual QA at 1440x980 and 390x844. | Backend suite passed with 150 tests. Existing kiosk frontend passed with 22 files and 187 tests, and production build passed, preserving the stable 5175 demo surface. Site suite passed with 1 file and 9 tests; site production build passed. Repository contract, spec coverage, staged-file safety, and provider secret scan passed. Playwright QA found no console errors, no desktop/mobile horizontal overflow, visible CTAs on desktop, separated mobile hero stacking, working video modal, form validation, and `/checkout/success` route. | Pass |
| Stabilized local voice demo flow | RED/GREEN targeted backend and frontend tests during implementation; final verification: `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`. | Backend suite passed with 141 tests. Frontend suite passed with 22 files and 187 tests. Frontend TypeScript/Vite build passed. Repository contract, 13 spec coverage checks, staged-file safety, and provider secret scan passed. `Relief Bomb` remains raw transcript evidence but corrects to `Relief Balm` before AI product flow, while raw `/api/products/search` still returns fuzzy candidates rather than authoritative facts. Successful TTS playback enters speaking state and returns ready after audio ends; browser autoplay block keeps the answer visible and offers `Tap to play voice` instead of `Try Again`. Empty, unsupported, malformed, and undecodable audio now return controlled `invalid_audio` responses without provider stack details. | Pass |
| Fuzzy ERP product search and animated Product enlarge | RED/GREEN targeted backend and frontend tests were run during implementation. Final verification: `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend`; `npm.cmd run build --prefix frontend`; `node scripts/check-repository.mjs`; `node scripts/check-specs.mjs`; `node scripts/check-staged-files.mjs`; `node scripts/check-provider-secrets.mjs`. | Backend suite passed with 135 tests. Frontend suite passed with 22 files and 185 tests. Frontend TypeScript/Vite build passed. Repository contract, 13 spec coverage checks, staged-file safety, and provider secret scan passed. Mock VitaFlow fuzzy search now returns branch-aware candidates for near matches such as `Relief Bomb` -> `Relief Balm` without creating a purchasing query, safety short-circuits before fuzzy/product flow, `/api/products/search` returns candidate metadata, the kiosk shows a customer-facing “Do you mean this item?” candidate panel, selecting a candidate updates Product/Shelf/Promotion/ERP display state, and the enlarged Product sheet toggles detail/summary by inside tap/click with a holographic morph marker while outside/Escape closes. | Pass |
| CI provider secret scan and clean frontend build fix | GitHub Actions run `28030164414` and fresh pushed run `28124474511` first failed at `Reject committed secret values` before tests. `gh run view` showed the exact failing file as `docs/architecture.md`; the old grep treated Markdown inline empty placeholder `OPENAI_API_KEY=` as a non-empty value because the closing backtick was matched. After that fix, run `28124743309` reached `Build frontend` and failed because clean CI lacked explicit Node typings for `frontend/src/devServerConfig.test.ts` imports of `node:fs`, `node:path`, and `node:url`. Local verification after both fixes: `node scripts/check-provider-secrets.mjs`, `python -m pytest backend/tests -v`, `npm.cmd run test:run --prefix frontend`, `npm.cmd run build --prefix frontend`, `node scripts/check-repository.mjs`, `node scripts/check-specs.mjs`, and `node scripts/check-staged-files.mjs` all passed. | Replaced the brittle bash grep in `.github/workflows/ci.yml` with `node scripts/check-provider-secrets.mjs`. The script scans tracked files for real non-empty `OPENAI_API_KEY`, `ELEVENLABS_API_KEY`, and `VITAFLOW_API_BASE_URL` assignments while allowing empty documentation placeholders. Added explicit frontend Node typings so CI clean install can type-check Node-based frontend config tests. CI remains mock-safe and does not require Ollama, faster-whisper model downloads, API keys, or live providers. | Pass |
| Fixed local VRM startup profile | RED targeted test `npm.cmd run test:run --prefix frontend -- src/devServerConfig.test.ts --reporter=dot` failed as expected while `VITE_TEXT_INPUT_MODE` was not injected by `dev:vrm`, `scripts/start-local-vrm-demo.ps1` was missing, and `docs/local-vrm-startup.md` was missing. GREEN targeted test passed with 1 file and 6 tests. PowerShell parser check for `scripts/start-local-vrm-demo.ps1` passed without executing the script. | Added a non-mutating Windows startup helper for the fixed local VRM demo. It requires local `.env` and `frontend/.env.local`, verifies `VITE_AVATAR_RENDERER=vrm`, `VITE_VRM_MODEL=vita-new`, `VITE_API_BASE_URL=http://127.0.0.1:8001`, `VITE_WS_BASE_URL=ws://127.0.0.1:8001`, and `VITE_TEXT_INPUT_MODE=native`, checks fixed ports 8001 and 5175, starts backend and `npm.cmd run dev:vrm --prefix frontend` in separate terminals, prints `http://127.0.0.1:5175`, and does not modify or print secrets. Added restart documentation in `docs/local-vrm-startup.md` and linked it from README/local demo docs. | Pass |
| Cylindrical carousel leaflet path | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx --reporter=dot` failed as expected while the outgoing active leaflet stayed pinned at `scale=1`, `translateZ=0px`, and `rotateY=0deg` during drag and side cards used the previous 62%/14deg geometry. GREEN targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` passed with 2 files and 30 tests. Full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 181 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec checks passed. | The enlarged leaflet deck now computes every card, including the outgoing active card, from one continuous moderate cylindrical path. Resting side cards use about `scale=0.720`, opacity `0.88`, `translateZ=-90px`, and `rotateY(22deg/-22deg)`; halfway drag moves outgoing/incoming cards to curved intermediate transforms instead of letting one slide underneath the other. In-app Browser QA at 1024x768 measured active `337-707`, previous `64-319`, next `725-980`, side gaps `18px`, no document horizontal overflow, no X/Previous/Next/dots/page counter, and metadata unchanged below the deck. Browser QA at 1366x768 measured active `348-718`, previous `75-330`, next `736-991`, side gaps `18px`, metadata unchanged at `1052-1352`, and no horizontal overflow. A real long drag from the third leaflet landed directly on the first leaflet. Screenshot evidence was emitted in the browser session; no screenshot files were created or committed. | Pass |
| Separated leaflet deck side cards | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while the active card still used 600px geometry, side cards used `scale=0.740`, `translateZ=-120px`, `rotateY(±20deg)`, and side-card slots overlapped the active footprint. GREEN targeted test with the same command passed with 2 files and 30 tests. Full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 181 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec checks passed. | The enlarged leaflet viewer now computes separated side-card slots from measured/viewport-safe scene width. Active leaflet remains centered and readable, side cards use `scale=0.620`, opacity `0.88`, `translateZ=-80px`, and outward `rotateY(±14deg)`, and the active card width adapts so previous/next cards sit beside it instead of underneath it. In-app Browser QA at 1024x768 measured active `321-724`, previous `82-294`, next `751-962`, side gaps `27px`, both side cards inside the scene, no document horizontal overflow, and no X/Previous/Next/dots/page counter. Browser QA at 1366x768 measured active `331-734`, previous `93-305`, next `761-973`, side gaps `26-27px`, metadata unchanged at `1052-1352`, and both side cards inside the scene. Long drag from the third leaflet selected the first leaflet directly. Screenshot evidence was emitted in the browser session; no screenshot files were created or committed. | Pass |
| Moderate cylindrical leaflet deck with multi-card swipe | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while the viewer still reported the old shallow pattern, side cards used the older size/depth values, and long drag only moved one card. GREEN targeted test with the same command passed with 2 files and 30 tests. Full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 181 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec checks passed. | The enlarged leaflet viewer now reports `data-deck-pattern="moderate-cylindrical"`, uses a larger 600px active hero card, keeps active scale at `1`, and renders side cards at `scale=0.740`, opacity `0.88`, `translateZ=-120px`, and outward `rotateY(±20deg)` under the current CSS perspective. Browser QA at 1024x768 measured previous/active/next cards inside the viewport, metadata outside the active leaflet, no X/Previous/Next/dots/page counter, and no document horizontal overflow. Browser QA at 1366x768 measured active `233-833`, previous `36-377`, next `689-1030`, metadata separate at `1052-1352`, and no side-card clipping. A real long drag from the third leaflet selected the first leaflet directly, proving multi-card distance-based navigation. Screenshot evidence was emitted in the browser session for 1024x768 and 1366x768; no screenshot files were created or committed. | Pass |
| Outward shallow cylindrical leaflet deck | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while the previous/left leaflet still used `rotateY(10deg)` and the next/right leaflet still used `rotateY(-10deg)`. GREEN targeted test with the same command passed with 2 files and 29 tests. Full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 180 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec checks passed. | The enlarged leaflet deck now keeps the same centered active card, metadata panel, side-card scale/depth, overlay layer, and no-button/no-dot interaction, but flips side orientation so previous/left opens outward with `rotateY(-10deg)` and next/right opens outward with `rotateY(10deg)`. In-app Browser visual QA at 1024x768 measured previous/active/next cards fully inside the overlay with side gaps of 109px, metadata outside the active leaflet, no document horizontal overflow, and no X/Previous/Next/dots. Browser QA at 1366x768 measured previous `28-270`, active `342-724`, next `796-1038`, side gaps of 72px, metadata separate at `1052-1352`, and outward side signs. Screenshot evidence was emitted in the browser session for 1024x768 and 1366x768; no screenshot files were created or committed. | Pass |
| Leaflet deck slot spacing and side-card reveal | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while production still used the previous partially overlapping deck values and width contract. GREEN targeted test with the same command passed with 2 files and 29 tests. | The enlarged leaflet deck now uses fixed left/center/right visual slots rather than a piled stack: active card width is constrained with `clamp(340px, 28dvw, 420px)`, side cards use `scale=0.720`, opacity `0.90`, `translateZ=-70px`, and `rotateY(±10deg)`, and slot spacing is calculated from active half-width + side half-width + a 48px visual gap before safe viewport clamping. In-app Browser QA at 1024x768 measured previous/active/next panels fully inside the viewport with side-card gaps of 48px on both sides; 1366x768 measured side-card gaps of 11px on both sides, metadata still separate at `1052-1352`, and no X/Previous/Next/dots/page counter. Screenshot evidence was emitted for 1024x768, 1366x768, and a 1366 metadata-region crop; no screenshot files were created or committed. | Pass |
| Visually distinct shallow cylindrical leaflet deck | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while production constants still used side opacity `0.72`, scale `0.820`, depth `-60px`, rotate `±10deg`, and the 1024px media rule widened cards to `58%`. GREEN targeted test with the same command passed with 2 files and 29 tests; full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 180 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec checks passed. | The enlarged leaflet viewer keeps the existing metadata panel unchanged and now uses a more visible shallow cylindrical wall: active card remains `scale=1`, `translateZ=0px`, and `rotateY=0deg`; previous/next cards use about `scale=0.780`, opacity `0.86`, `translateZ=-90px`, and `rotateY(±16deg)`. In-app Browser QA at 1024x768 measured previous/active/next cards fully inside the viewport with side card rects `62-385` and `659-983`; 1366x768 measured side card rects `72-396` and `670-994`, metadata still separate at `1052-1352`, and no visible side-card clipping. Screenshot evidence was emitted in the browser session for 1024x768 and 1366x768; no screenshot files were created or committed. | Pass |
| Product enlarged scrollbar-flash fix | RED targeted test `npm.cmd run test:run --prefix frontend -- src/typedInputLayout.test.ts --reporter=dot` failed as expected while the Product enlarged decorative sweep still used transform-based animation. GREEN targeted test `npm.cmd run test:run --prefix frontend -- src/components/ProductCard.test.tsx src/typedInputLayout.test.ts --reporter=dot` passed with 2 files and 26 tests. Full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 180 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec checks passed. | Product-only browser audit on `http://127.0.0.1:5175` measured detail and summary enlarged sheets at 1024x768 and 1366x768. Immediate, settled, and after-close states all reported document/body horizontal overflow `0`; `.product-viewer-stage` reported `scrollWidth == clientWidth` for detail and summary; fact/summary cards stayed inside the stage; `html`/`body.product-expanded` were applied only while the Product overlay was open. A 1024x768 in-app browser proof frame showed no bottom white scrollbar line; no screenshot files were created or committed. | Pass |
| Shallow cylindrical leaflet deck | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while the viewer still reported `data-deck-pattern="flat-horizontal"`, side cards used flat `scale=0.780` with no depth/rotate variables, and CSS had `perspective: none`. GREEN targeted test with the same command passed with 2 files and 29 tests; full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 179 tests; frontend build `npm.cmd run build --prefix frontend` passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec/staged checks passed. | Enlarged leaflet viewer now reports `data-deck-pattern="shallow-cylindrical"`, uses a top-level fixed overlay with `perspective: 1200px` and `transform-style: preserve-3d`, keeps the active card at `scale=1`, `translateZ=0px`, and `rotateY=0deg`, and positions neighboring cards at `x=±308px`, `scale=0.820`, `translateZ=-60px`, and `rotateY=±10deg`. In-app Browser DOM/bounding-box QA at 1280x720 confirmed previous/next visible cards stayed inside the viewport, offscreen cards were hidden at `opacity=0`, no X/Previous/Next/dots/page counter were present, and the existing metadata panel was unchanged and separate from the deck. No screenshots were created or committed. | Pass |
| Smooth leaflet deck swipe and side-card clipping fix | RED targeted tests `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` and `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx --reporter=dot` failed as expected while side offsets were still `-393px`/`393px`, the active card scaled to `0.933` during drag, the swipe scene used `overflow: hidden`, and offscreen cards remained partially visible at `opacity=0.11`. GREEN targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` passed with 2 files and 29 tests. | Leaflet deck now constrains side-card offsets to the measured stage/card safe area, keeps the current active leaflet at `--leaflet-deck-scale: 1` during drag, sets the swipe scene to `overflow: visible`, and hides non-neighbor/offscreen cards with opacity `0`. In-app Browser DOM/bounding-box QA at 1280x720 showed first-card and middle-card visible panels fully inside the viewport, `sceneOverflow="visible"`, active style `--leaflet-deck-scale: 1`, offscreen opacity `0`, no X/Previous/Next/dots, and a real drag left the remaining visible cards inside the viewport. Metadata panel layout/content was not redesigned. No screenshots were created or committed. | Pass |
| Enlarged leaflet deck hero sizing and no-bounce swipe | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed as expected while the enlarged deck still used the old 1080px/650px stage, 44%/520px leaflet width, side offsets around `-277px`/`277px`, side scale `0.820`, and the opening animation still contained `translateY(-4px) scale(1.025)`. GREEN targeted test with the same command passed with 2 files and 27 tests. | Enlarged leaflet viewer remains a flat horizontal floating deck with metadata unchanged. The overlay stage now uses `width: min(1560px, 96dvw)` and `height: min(760px, 88dvh)`; the active card uses `width: min(56%, 760px, 52dvw)` and `height: min(100%, 78dvh)`; real side cards remain visible at `-393px`/`393px`, scale `0.780`, and opacity `0.72`. Card transitions use a controlled 320ms cubic-bezier tween, the opening animation has no overshoot keyframe, and CSS contains no `scale(1.025)` or `translateY(-4px)` bump. No screenshot artifacts were created or committed for this task. | Pass |
| Localized Product summary and holographic enlarge | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/ProductCard.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed with 7 expected failures while the Product summary still showed `Back to product details`, `Use`, non-localized summary fields, fixed summary columns, and no enlarged Product dialog. GREEN targeted test passed with 2 files and 25 tests; full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 177 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec/staged checks passed. | Product summary now uses localized EN/中文/BM labels and safe localized summary text with field-level English fallback, keeps Product name/SKU/price/branch/shelf/stock/source facts untranslated, removes visible `Back to product details`, changes `Use` to `How to use`, wraps summary cards with an auto-fit responsive grid, and supports click/tap/keyboard summary toggling plus double-click/double-tap enlarged detail or summary holographic sheets. The enlarged Product sheet is a top-level floating layer, closes on outside click or Escape, keeps inside clicks open, has no visible X button, and uses reduced-motion-safe transform/opacity animation. No screenshots were created or committed for this task. | Pass |
| Corrected flat leaflet deck with metadata unchanged | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx --reporter=dot` failed as expected while side leaflets were offset to `-648px`/`648px`, scaled `0.860`, and opacity `0.62`. GREEN targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` passed with 27 tests; full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 172 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository and spec checks passed. | Leaflet deck only was corrected: active leaflet stays centered at `0px`/scale `1`, previous/next leaflets use flat side offsets `-277px`/`277px`, scale `0.820`, and opacity `0.72`; CSS locks card width to `min(44%, 520px)` so side cards remain visible in the overlay. The existing metadata panel remains separate from `.leaflet-flat-deck-track`, keeps its title/branch/source/validity/description behavior, and was not moved, attached, removed, or redesigned. No screenshots were created or committed. | Pass |
| Flat horizontal leaflet swipe deck | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/LeafletModal.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed with 5 expected failures while the viewer lacked animation state, still used `.leaflet-depth-track`, hid side leaflets, and closed instantly. GREEN targeted test passed with 2 files and 26 tests; full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 171 tests; frontend build `npm.cmd run build --prefix frontend` passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository and spec checks passed. | Enlarged promotion/campaign leaflets now render in a top-level fixed overlay as a Flat Horizontal Floating Deck: active leaflet centered and fully visible, real previous/next leaflets visible as left/right flat neighbors, horizontal drag/swipe/trackpad and keyboard fallback supported, no X/Previous/Next/dots/page counter/header/swipe text, metadata stays outside artwork, and outside click/Escape use a smooth holographic collapse instead of instant close. No screenshot artifacts were created or committed for this task because screenshots were not explicitly approved. | Pass |
| Futuristic shelf map viewer and product summary transform | RED targeted test `npm.cmd run test:run --prefix frontend -- src/components/ProductCard.test.tsx src/components/ShelfMap.test.tsx src/typedInputLayout.test.ts --reporter=dot` failed with 8 expected failures while Shelf Navigation had no enlarged viewer, Product had no summary transform, and CSS contracts were missing. GREEN targeted test passed with 3 files and 25 tests. Full frontend suite `npm.cmd run test:run --prefix frontend` passed with 22 files and 170 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec/staged checks passed. In-app Browser measured 1024x768 and 1366x768 on `http://127.0.0.1:5175`. | Shelf Navigation now opens a top-level fixed holographic map viewer from click/tap/keyboard activation; measured overlay uses `position: fixed`, `z-index: 880`, route summary remains inside the stage, and outside click closes while inside click is retained by component tests. Product panel now toggles to a source-safe summary state with Ingredient, Use, Best for, Size, and Description using a transform-shell morph rather than a 180-degree flip; measured 5/5 summary fields remained inside the Product panel at 1024x768 and 1366x768. Screenshots: `reports/evidence/shelf-map-enlarged-viewer-1024x768.png`, `reports/evidence/shelf-map-enlarged-viewer-1366x768.png`, `reports/evidence/product-summary-transform-1024x768.png`, and `reports/evidence/product-summary-transform-1366x768.png`. | Pass |
| Single-leaflet promotion panel and responsive enlarged viewer | RED targeted tests `npm.cmd run test:run --prefix frontend -- PromotionPoster typedInputLayout` and `npm.cmd run test:run --prefix frontend -- typedInputLayout` failed while the collapsed promotion panel still expected multi-leaflet cards and while collapsed/enlarged leaflet artwork lacked measured containment contracts. GREEN targeted tests passed with `PromotionPoster`, `typedInputLayout`, and `App.integration.test.tsx` (48 tests); full frontend suite `npm.cmd run test:run --prefix frontend` passed with 21 files and 162 tests; frontend build passed; backend suite `python -m pytest backend/tests -v` passed with 125 tests; repository/spec/staged checks passed. | Normal promotion panel now renders exactly one visible `.leaflet-poster` while retaining `data-leaflet-count="3"` for the enlarged viewer data source. Browser measurements at 1024x768, 1366x768, 1920x1080, and 900x768 reported one visible leaflet poster, no collapsed title/description/branch/source/validity text, no horizontal overflow, poster/stage/image fully inside the Promotion panel, and `object-fit: contain`. Enlarged viewer measurements at 1366x768 showed metadata beside the leaflet with no scene/metadata or active/metadata overlap; 880x640 stacked metadata below the hero leaflet with no overlap, no legacy modal box, no visible X/Previous/Next/dots/header/swipe text, no visible buttons, click-outside close passed, and active/image bounds were inside the stage. Screenshot evidence: `reports/evidence/promotion-single-leaflet-1024x768.png`, `reports/evidence/promotion-single-leaflet-1366x768.png`, `reports/evidence/promotion-single-leaflet-1920x1080.png`, `reports/evidence/promotion-single-leaflet-narrow-900x768.png`, `reports/evidence/leaflet-floating-viewer-1366x768.png`, and `reports/evidence/leaflet-floating-viewer-narrow-880x640.png`. | Pass |
| Promotion leaflet responsive display and viewer non-overlap | RED targeted test `npm.cmd run test:run --prefix frontend -- PromotionPoster typedInputLayout` failed with 5 expected failures while the normal promotion panel rendered only one leaflet and the CSS contract lacked responsive leaflet grid / 1120px+740px viewer reflow rules. GREEN targeted test passed with 19 tests; full frontend suite, frontend build, backend suite, repository/spec/staged checks; in-app Browser bounding-box QA at 1024x768, 1280x720, 1366x768, 1440x900, 1920x1080, 2560x1440 large-column, and 880x640 squeezed viewer. | Normal promotion panel now renders ordered active branch-valid leaflet candidates, hides extra leaflets by container query, and uses the leaflet card itself as the touch target. At 1024/1280/1366/1440/1920 the visible primary leaflet filled about 85-86% of the panel with no document overflow; at 2560x1440 the wider promotion column displayed 2 leaflets. Product-linked promotion remained first; product-without-promotion and product-not-found flows kept campaign first. Enlarged viewer measurements showed no active leaflet/metadata overlap and no scene/metadata overlap at 1024x768, 880x640, 1280x720, 1366x768, 1440x900, and 1920x1080. Stacked layouts used `overflow: hidden` on the swipe scene with 74px+ scene-to-metadata gap; wide layouts kept metadata beside the scene. No X, Previous/Next, dots, page counter, modal title, particles, fog, smoke, or heavy modal container were present. Screenshot evidence was saved outside the repository and not committed. | Pass |
| Leaflet display, shelf readability, and viewer reflow refinement | RED full frontend suite `npm.cmd run test:run --prefix frontend` failed with 8 expected failures: visible `Enlarge Leaflet`, product-without-promotion not defaulting to campaign, product-not-found not defaulting to campaign, gallery cards using nested `View leaflet`, new leaflet action aliases ignored, old Shelf panel min-height contract, and no 920px/680px viewer metadata reflow contract. GREEN full frontend suite passed with 21 files and 159 tests; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec/staged checks; in-app Browser bbox QA at 1024x768, 1366x768, 1920x1080, and 880x640 squeezed viewer. | Promotion panel now shows leaflet artwork/cards directly with no visible `Enlarge Leaflet`; leaflets are direct touch targets; product-linked promotion still appears first; product-without-promotion and product-not-found states default to an active campaign leaflet; frontend accepts controlled aliases `OPEN_PROMOTION_LEAFLET`, `OPEN_CAMPAIGN_LEAFLET`, and `SHOW_LEAFLET_GALLERY` while ignoring arbitrary actions. Browser QA measured no horizontal/vertical overflow at 1024x768, 1366x768, or 1920x1080; Shelf route/facts/map/current marker/target marker are inside the Shelf panel and route does not intersect the typed rail. Leaflet viewer QA measured no metadata overlap at 1024x768 and 1366x768; at 880x640 metadata reflowed below the hero leaflet with about 35px separation; no X, Previous/Next, dots, visible page counter, top title, particles, fog, smoke, or heavy modal container were present. Click inside kept the viewer open; click outside closed it. No screenshots were committed. | Pass |
| Backend health | `Invoke-WebRequest http://127.0.0.1:8000/health` | `/health` returns status, service, `provider_mode=mock`, and provider summary with mock STT/TTS/AI/VitaFlow/vision selectors | Pass |
| Backend tests | `python -m pytest backend/tests -v` | 125 passed | Pass |
| Controlled provider config | Included in `python -m pytest backend/tests -v` | 7 tests passed: provider selectors default to mock, credentials/settings do not auto-enable live providers, OpenAI Whisper STT requires explicit selector, missing `OPENAI_API_KEY` fails closed, faster-whisper requires explicit selector, and invalid selectors fail closed | Pass |
| Frontend tests | `npm.cmd run test:run --prefix frontend` | 20 files, 153 tests passed | Pass |
| Audio-reactive assistant waveform and subtle background motion | RED targeted test `npm.cmd run test:run --prefix frontend -- AvatarAssistant.test.tsx useVoiceInteraction.test.ts typedInputLayout.test.ts` failed with 11 expected failures while the waveform had no state telemetry, no microphone RMS exposure, and no lightweight animation contract; GREEN targeted test passed with 3 files and 58 tests; in-app Browser QA on `http://127.0.0.1:5175/?waveformAudit=idle`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec checks | The assistant waveform now exposes state-aware modes for idle `breathing`, listening `microphone`, thinking `scanning`, speaking `playback`, error `warning`, and pharmacist escalation `safety`. Listening uses microphone analyser RMS from the existing silence detector; speaking uses playback analyser activity with a subtle simulated waveform fallback when no analyser value is available. Browser QA reported 25 waveform bars, idle `data-visual-activity="0.12"`, background animations `kiosk-background-drift` and `kiosk-ambient-grid-drift`, assistant sweep `assistant-state-sweep`, typed interaction changed the waveform to `thinking/scanning`, controlled local provider failure changed it to `error/warning`, and Start reset returned it to `idle/breathing`. No backend product, VitaFlow, provider, promotion, safety, or WebSocket logic changed. | Pass |
| Language selector and debug-label cleanup | RED targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx AvatarAssistant.test.tsx useVoiceInteraction.test.ts`; GREEN targeted test with the same command; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v` | Tests first failed because runtime/avatar diagnostics were visible by default, no footer language selector existed, and AI requests did not include a language preference. Green tests prove `Renderer`, `Model`, `Avatar`, `AI`, and `STT` diagnostics are hidden unless `VITE_SHOW_DEBUG_STATUS=true`; footer shows `Connected | EN 中文 BM`; UI defaults to EN while sending `preferredLanguage=auto`; selecting 中文/BM translates major kiosk labels, persists to localStorage, and sends `zh`/`ms`; product name, SKU, price, shelf code, branch code, and `Mock VitaFlow` remain unchanged. Backend tests prove `preferred_language` is accepted without changing VitaFlow/mock product facts and Ollama prompt context receives manual language preference. | Pass |
| Explicit VRM renderer config | RED targeted test `npm.cmd run test:run --prefix frontend -- AvatarRenderer.test.ts AvatarAssistant.test.tsx VrmAvatarRenderer.test.tsx AvatarModel.test.ts`; GREEN targeted test with the same command; local Vite runtime checks on ports 5178 and 5179 | Tests first failed because plain `AVATAR_RENDERER` still selected renderers, the dev renderer badge was missing, and VRM fallback produced no console warning. Targeted tests then passed with 5 files and 51 tests: `VITE_AVATAR_RENDERER=vrm` selects the VRM renderer, `VITE_VRM_MODEL=vita-new` resolves the alternate local VRM model, missing env keeps Lottie/holographic fallback, plain `AVATAR_RENDERER` is ignored, and VRM fallback logs a developer warning with `reason=missing-vrm-model-url`. Runtime check with `VITE_AVATAR_RENDERER=vrm` and `VITE_VRM_MODEL=vita-new` reported `renderer=vrm`, `model=vrm`, `modelKey=vita-new`, `modelUrl=/src/assets/avatar/vita-new.vrm`, `lottieCount=0`, and debug badge `Renderer: vrm`; runtime check without the env reported `renderer=lottie`, `vrmCount=0`, and debug badge `Renderer: lottie`. | Pass |
| Explicit VRM local startup helper | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- devServerConfig.test.ts`; runtime check `npm.cmd run dev:vrm --prefix frontend`; in-app Browser at `http://127.0.0.1:5175` | Browser QA showed the currently running old 5175 process was still `Avatar: lottie` / `Renderer: lottie`, confirming the startup issue was an old Vite server without VRM env rather than a deleted model. The new `dev:vrm` helper injects `VITE_AVATAR_RENDERER=vrm`, `VITE_VRM_MODEL=vita-new`, `VITE_API_BASE_URL=http://127.0.0.1:8001`, and `VITE_WS_BASE_URL=ws://127.0.0.1:8001` before Vite starts. Because PID 18964 already occupied strict port 5175, the helper failed clearly with `Port 5175 is already in use` and printed the PowerShell listener check instead of silently switching ports. | Pass |
| Local AI + VRM runtime status profile | RED/GREEN targeted tests `python -m pytest backend/tests/test_health.py -v` and `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx AvatarAssistant.test.tsx devServerConfig.test.ts`; full backend suite `python -m pytest backend/tests -v`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend` | Tests first failed because `/api/runtime/status`, `check_ollama_reachable`, controlled provider-unavailable copy, missing `VITE_AVATAR_RENDERER` warning, and `scripts/start-local-ai-demo.ps1` did not exist. Green tests now prove `/api/runtime/status` returns only safe provider fields and no API keys, `.models` paths, private URLs, or secrets; local Ollama/faster-whisper profile can report `ollama_reachable=true` through a mocked reachability check; the frontend diagnostics badge reads backend runtime status and shows `Provider status unavailable` instead of `UNKNOWN` when fetch fails; dev mode warns when `VITE_AVATAR_RENDERER` is missing; and the PowerShell helper starts backend 8001 plus frontend 5175 without writing `.env`. | Pass |
| Accessibility typed input workflow | RED targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx inputConfig.test.ts useVoiceInteraction.test.ts` failed first with 8 expected old-flow failures; GREEN targeted test passed with 3 files and 31 tests; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend` | App integration proves typed input renders below Shelf navigation, defaults to native keyboard mode, does not force a popup on focus or Type / Keyboard button in native mode, supports Chinese and Malay typed text through normal input, opens the full-screen popup only in explicit popup mode, preserves drafts on close, sends/clears from popup, reset clears typed text and closes popup, and Tap to Speak remains unaffected. Hook tests prove typed pregnancy questions escalate without TTS or purchasing query, while unknown typed products stay in purchasing query flow without guessed product facts. Full frontend suite passed with 17 files and 106 tests; frontend build passed. | Pass |
| Compact typed input rail | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx typedInputLayout.test.ts`; in-app Browser at `http://127.0.0.1:5175`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v` | Regression tests first failed while the normal typed input was still a large panel with `Accessible input`, `Native keyboard mode`, and `Type / Keyboard`. Green tests now prove the normal typed input is a `data-layout="compact-rail"` single-line input below Shelf Navigation, removes the large title/badge/card button, keeps native focus behavior, opens the larger typing screen only from the compact keyboard icon, preserves draft, clears/sends safely, and protects the Shelf Navigation row with a fixed compact rail height. Browser QA measured 1024x768, 1366x768, and 1920x1080 with no horizontal overflow, no landscape document scrolling, no old panel copy visible, and Shelf Navigation fully above the compact rail; at 1024x768 the Shelf map measured 291px tall and the input rail 54px tall. Full frontend suite passed with 19 files and 111 tests; backend suite passed with 121 tests. | Pass |
| Typed input rail-only responsive correction | Targeted test `npm.cmd run test:run --prefix frontend -- typedInputLayout.test.ts`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; browser measurements at 1024x768, 1280x720, 1366x768, 1440x900, and 1920x1080 on `http://127.0.0.1:5175`; screenshots `reports/evidence/typed-input-rail-constrained-1024x768.png` and `reports/evidence/typed-input-rail-constrained-1366x768.png` | The rejected fixed-canvas/global-shrink approach was removed: no `.kiosk-viewport`, no 4:3 fixed canvas, no whole-app transform scaling, and the main shell fills the viewport. The compact typed rail remains 48-64px tall, the input field flexes and ellipsizes, the keyboard icon stays 52px wide, Send stays 72px wide, and the rail does not overlap or compress Shelf Navigation. Browser QA also confirmed no horizontal overflow, no normal landscape document scrolling, Shelf map inside its panel, product facts inside the product card, and the main kiosk panels readable at all measured landscape viewports. Full frontend suite passed with 19 files and 121 tests; frontend build passed; backend suite passed with 123 tests. | Pass |
| Full typing modal overlay | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx typedInputLayout.test.ts`; in-app Browser at 1024x768 on `http://127.0.0.1:5175`; screenshot `reports/evidence/typed-input-full-modal-1024x768.png`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v` | Regression test first failed while the typing screen lacked a viewport-level overlay contract. Green tests now prove the compact keyboard icon opens a top-level `data-overlay-root="typing-screen"` outside both the Shelf Navigation map and the compact input rail, focuses the large textarea, preserves the compact draft, keeps Close/Done/Clear/Send actions, and keeps the popup out of the normal kiosk grid row. Browser QA showed the overlay covers the full 1024x768 viewport, the dialog is near full-screen at about 996x660, the overlay parent is `document.body`, Shelf/Input containers do not contain it, and console had no relevant warnings or errors. Full frontend suite passed with 19 files and 117 tests; backend suite passed with 123 tests. | Pass |
| Real virtual keyboard typing modal | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx typedInputLayout.test.ts`; in-app Browser at 1024x768 on `http://127.0.0.1:5175`; screenshot `reports/evidence/typed-input-virtual-keyboard-1024x768.png`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v` | Regression tests first failed because the typing modal only contained a large textarea and no `English virtual keyboard` or `Type Q` controls. Green tests now prove the compact keyboard icon opens the full modal with a visible EN QWERTY keyboard, Space and Backspace update the draft, Close/Done preserves draft, Clear clears modal and compact input, Send uses the same typed workflow, external/native keyboard input remains enabled, Chinese text entry remains available through the textarea/native IME path, no Chinese virtual keyboard mode or phrase candidates are shown, and voice flow remains unaffected. Browser QA showed the real EN keyboard area visible below the textarea and console had no relevant warnings or errors. Full frontend suite passed with 19 files and 120 tests; backend suite passed with 123 tests. | Pass |
| EN virtual keyboard backup | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx inputConfig.test.ts typedInputLayout.test.ts`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v` | Regression tests first failed while the popup still exposed a `Chinese keyboard` mode, a device-IME panel, and a keyboard language preference. Green tests now prove native input remains the default, the compact keyboard icon intentionally opens the modal, the EN QWERTY backup keyboard remains available with Space/Backspace/Clear/Send/Close-Done behavior, Chinese text can still be typed through the textarea/native IME path, and no Chinese virtual keyboard mode, pinyin candidate keyboard, or hardcoded phrase dictionary ships in the customer UI. Full frontend suite passed with 19 files and 122 tests; frontend build passed; backend suite passed with 123 tests. | Pass |
| Removed custom Chinese keyboard toggle | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx inputConfig.test.ts typedInputLayout.test.ts`; in-app Browser QA on `http://127.0.0.1:5175`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec checks | Regression tests first failed while the typing modal still exposed a keyboard language preference boundary. Green tests now prove the modal has no `Input language preference` group, no `English keyboard` toggle, no `Chinese keyboard` toggle, no BM toggle, no Chinese candidate/pinyin UI, and no hardcoded Chinese phrase dictionary. Browser QA confirmed one `English virtual keyboard` group, zero language preference groups, no visible `中文`, textarea enabled and not readonly, and the helper text `Use the device keyboard for Chinese pinyin or external keyboard input.` Full frontend suite passed with 19 files and 122 tests; frontend build passed; backend suite passed with 123 tests; repository contract and spec coverage passed. | Pass |
| Focused typing modal non-overlap layout | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- typedInputLayout`; browser layout audit at 1024x768 on `http://127.0.0.1:5175`; screenshot `reports/evidence/typing-modal-no-overlap-1024x768.png` | Regression test first failed because the modal used a crowded seven-row grid and an extra visual note row. Green test now locks the modal to header, textarea, helper, EN keyboard, and actions grid areas; removes the extra visual note; keeps the helper, keyboard, and Clear/Done/Send action rows in normal flow; and forbids absolute-position/negative-margin overlap hacks for those rows. Browser measurement showed 8px gaps between textarea/helper/keyboard/actions, no overlap pairs, all modal children contained inside the dialog, one EN keyboard group, zero modal Chinese toggles, 28 EN keys, and textarea `readonly=false`. | Pass |
| UI badge overlap and icon clipping polish | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx typedInputLayout.test.ts`; in-app Browser QA on `http://127.0.0.1:5175` at 1024x768, 1366x768, and 1920x1080 | Regression tests first failed because runtime diagnostics were still outside the header/floating over the app and artwork containers still allowed clipping. Green tests and browser bounding-box checks prove diagnostics are inside the `.kiosk-header`, `position=relative`, and do not overlap `.ai-subtitle-panel`; product glyphs stay inside `.product-art`; leaflet images use `object-fit: contain`; and enlarged leaflet artwork is contained by safe modal/gallery bounds at all three viewport sizes. | Pass |
| Measured subtitle/Product collision fix | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- typedInputLayout.test.ts`; in-app Browser bounding-box measurements at 1024x768, 1366x768, and 1920x1080 on `http://127.0.0.1:5175`; screenshots `reports/evidence/subtitle-product-overlap-1024x768.png` and `reports/evidence/subtitle-product-overlap-1366x768.png`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec checks | Before measurement showed subtitle-to-product visual gap 4.42px at 1024x768, 12.94px at 1366x768, and 26.40px at 1920x1080. A first gap-only fix still measured only 7.99px at 1024x768, exposing the real cause: the first `.clinical-deck` grid row was smaller than the subtitle card. Final measurement uses a protected deck gap and a larger first row: 17.24px at 1024x768, 15.06px at 1366x768, and 22.87px at 1920x1080. Product source badge is inside both `.product-panel` and `.panel-title-row`, product icon is inside `.product-art`, every product fact `dt`/`dd` has positive visible area inside the Product panel, and no important measured element has zero or negative area. Full frontend suite passed with 19 files and 126 tests; frontend build passed; backend suite passed with 123 tests; repository contract and spec coverage passed. | Pass |
| Product panel responsive clipping fix | RED targeted test `npm.cmd run test:run --prefix frontend -- typedInputLayout.test.ts` failed until the Product panel contract required auto height, visible important overflow, protected min-height, auto-fit facts, and a bounded decoration; browser bounding-box audit at 1024x768, 900x768, 1366x768, and 1920x1080 on `http://127.0.0.1:5175`; screenshots `reports/evidence/product-panel-no-clipping-1024x768.png`, `reports/evidence/product-panel-no-clipping-900x768.png`, and `reports/evidence/product-panel-no-clipping-1366x768.png`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec checks | Before measurement showed Product panel clipping risk: 1024x768 `clientHeight=174` / `scrollHeight=244`, 900x768 `174/244`, and 1366x768 `195/273`, with the decorative `::after` also creating horizontal scroll width. Final measurement shows no Product panel scroll clipping: 1024x768 `clientHeight=218` / `scrollHeight=218`, 900x768 `218/218`, 1366x768 `218/218`, and 1920x1080 `298/298`; `scrollWidth==clientWidth` at all measured sizes, source badge and product art are fully inside the Product panel, every Stock/Branch/Shelf/Source card and its `dt`/`dd` text has full visible area inside the panel, 900x768 wraps facts into a clean 2x2 grid, Product-to-Shelf gap remains at least 16px, and document overflow remains zero. Full frontend suite passed with 19 files and 127 tests; frontend build passed; backend suite passed with 123 tests; repository contract and spec coverage passed. | Pass |
| Shelf Navigation route clipping fix | RED targeted test `npm.cmd run test:run --prefix frontend -- typedInputLayout.test.ts` failed until the Shelf panel contract required a protected min-height, grid rows for header/map/facts/route, visible panel overflow for important content, route line-height/min-height, and a later typed-rail wrap threshold; browser bounding-box audit at 1024x768, 900x768, 1366x768, and 1920x1080 on `http://127.0.0.1:5175`; screenshots `reports/evidence/shelf-route-no-clipping-1024x768.png`, `reports/evidence/shelf-route-no-clipping-900x768.png`, and `reports/evidence/shelf-route-no-clipping-1366x768.png`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec checks | Before measurement showed Shelf panel clipping: 1024x768 `clientHeight=180` / `scrollHeight=268`, 900x768 `180/268`, 1366x768 `224/271`, and 1920x1080 `334/372`; the route row was outside the panel with bottom gaps of `-76.91px`, `-76.91px`, `-34.38px`, and `-19.92px`, and at 1024/900/1366 it intersected the typed input rail. Final measurement shows no Shelf panel scroll clipping: 1024x768 `242/242`, 900x768 `242/242`, 1366x768 `242/242`, and 1920x1080 `334/334`; route row, Aisle/Shelf/Level facts, You-are-here marker, and target marker are fully inside the Shelf panel; route-to-panel bottom gap is `13.29px`, `13.29px`, `13.28px`, and `18.27px`; panel-to-typed gap remains at least 16px; and document overflow remains zero. Full frontend suite passed with 19 files and 128 tests; frontend build passed; backend suite passed with 123 tests; repository contract and spec coverage passed. | Pass |
| Typed input responsive wrap | RED targeted test `npm.cmd run test:run --prefix frontend -- typedInputLayout.test.ts` failed while the rail still used a fixed-height/flex-only contract; GREEN targeted test passed; in-app Browser measurements at 1024x768 and 1366x768 on `http://127.0.0.1:5175`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v`; repository/spec checks | CSS contract now requires the compact rail to stay in normal flow with `height: auto`, visible overflow, and grid areas. Browser QA at 1024x768 confirmed the constrained center column wraps into two rows (`input input input` / `keyboard clear send`), rail height expands to about 103px, Shelf Navigation bottom is above the rail top, keyboard is 52px, Send is 72px, no horizontal overflow, and no overlap with Shelf/Product/Promotion/ERP. At 1366x768 the rail remains one row, about 52px tall, with keyboard/Clear/Send visible and no panel overlap. The typing modal still opens from the keyboard icon. Full frontend suite passed with 19 files and 122 tests; frontend build passed; backend suite passed with 123 tests; repository contract and spec coverage passed. | Pass |
| Whisper STT provider wiring | `python -m pytest backend/tests/test_openai_stt.py backend/tests/test_provider_config.py backend/tests/test_api.py::test_transcribe_accepts_audio backend/tests/test_services.py::test_mock_stt_returns_deterministic_transcript -v`; `npm.cmd run test:run --prefix frontend -- useVoiceInteraction.test.ts` | Backend tests prove OpenAI Whisper STT is selected only by `STT_PROVIDER=openai_whisper`, reads `OPENAI_API_KEY` from environment, posts multipart audio to the OpenAI transcription endpoint through a mocked `httpx` transport, returns transcript/language/clarification metadata, detects English/Chinese/Malay/mixed speech, handles unclear transcript without guessing, and surfaces safe request errors without leaking provider details; frontend tests prove unclear speech asks the customer to try again and does not call AI response or TTS | Pass |
| Local faster-whisper STT correction | `python -m pytest backend/tests/test_faster_whisper_stt.py backend/tests/test_provider_config.py backend/tests/test_api.py::test_transcribe_accepts_audio backend/tests/test_openai_stt.py -v`; full suite `python -m pytest backend/tests -v`; `npm.cmd run test:run --prefix frontend -- useVoiceInteraction.test.ts` | Targeted backend tests passed: `STT_PROVIDER=faster_whisper` is explicit-only, no real model is run in tests, English/Chinese/Malay/mixed Malaysian speech is detected, `pana doll` corrects to `Panadol`, `probio gut` corrects to `ProbioGut`, `ubat batok` corrects to `ubat batuk`, low-confidence and unclear transcripts request clarification, and existing red-flag text still escalates through safety before product recommendation. Frontend hook test proves corrected transcript is used for the AI/safety workflow while raw transcript remains stored internally. | Pass |
| Pregnancy safety gate before product flow | RED/GREEN targeted tests `python -m pytest backend/tests/test_ai_brain.py::test_pregnancy_safety_questions_escalate_before_product_flow backend/tests/test_services.py::test_pregnancy_and_breastfeeding_terms_require_pharmacist_review backend/tests/test_faster_whisper_stt.py::test_pregnancy_transcript_escalates_without_purchasing_query backend/tests/test_faster_whisper_stt.py::test_product_dictionary_correction_uses_mock_pharmacy_terms backend/tests/test_ai_brain.py::test_red_flag_returns_only_pharmacist_action backend/tests/test_ai_brain.py::test_unknown_product_creates_one_purchasing_query -v`; full suite `python -m pytest backend/tests -v`; manual local `STT_PROVIDER=faster_whisper` API verification on `127.0.0.1:8001` | Tests first reproduced the blocker, then passed after the fix. Pregnancy and breastfeeding terms in English, Malay, and Chinese now return `intent=red_flag`, `safety_reason=pregnancy_safety`, `REQUEST_PHARMACIST_ASSISTANCE`, no product, no promotion/leaflets, no VitaFlow product lookup, and no purchasing query. Manual faster-whisper verification returned `provider=faster_whisper` for "I am pregnant, can I take this supplement?", then AI returned the safe pharmacist handoff message and `purchasing_query_id=null`; "Ada ubat batuk?" corrected `Aida you bat batuck` to `ada ubat batuk`. | Pass |
| Local Ollama AI provider | RED/GREEN targeted tests `python -m pytest backend/tests/test_ollama_ai.py backend/tests/test_provider_config.py backend/tests/test_services.py::test_high_risk_condition_terms_escalate_to_pharmacist -v`; full backend suite `python -m pytest backend/tests -v`; `ollama pull qwen2.5:7b`; `ollama list`; manual local `AI_PROVIDER=ollama` verification against `http://127.0.0.1:11434` with `OLLAMA_MODEL=qwen2.5:7b`; HTTP backend verification on `http://127.0.0.1:8001/api/ai/respond` | Tests prove mock AI remains default, `AI_PROVIDER=ollama` selects `OllamaAIBrain` without a network call at startup, local Ollama offline errors fall back to deterministic mock AI, Ollama JSON is schema-validated, string or object UI actions are accepted only when whitelisted, unknown UI actions are rejected, unsafe or invented price/stock/shelf/medical wording is not used, product facts remain VitaFlow/mock-sourced, pregnancy safety escalates before any Ollama request or product flow, and English/mixed/Malay language context is passed to the local model prompt. Manual local check downloaded `qwen2.5:7b` successfully; Ollama API returned `200`; real qwen responses returned `source=ollama` for Panadol unknown-product purchasing query, Chinese/mixed promotion gallery wording, Panadol stock unknown-product purchasing query, and Malay `ubat batuk` unknown-product purchasing query. Pregnancy returned `red_flag` / `pregnancy_safety`, `REQUEST_PHARMACIST_ASSISTANCE`, and no purchasing query before normal model flow. | Pass |
| Local Ollama + VRM demo profile | RED/GREEN targeted tests `python -m pytest backend/tests/test_health.py -v` and `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx AvatarAssistant.test.tsx VrmAvatarRenderer.test.tsx AvatarModel.test.ts`; full backend suite `python -m pytest backend/tests -v`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend` | Tests first failed because `/health` did not expose provider summary, the app had no local diagnostics badge, and VRM model debug evidence was missing. Green tests now prove `/health` reports provider selectors, `/api/runtime/status` reports safe provider status, dev mode shows `AI: ollama`, `STT: faster_whisper`, `Avatar: vrm`, and `VRM: vita-new`, `VITE_AVATAR_RENDERER=vrm` and `VITE_VRM_MODEL=vita-new` render the selected local VRM path independently of backend provider mode, Lottie remains default when the frontend env is missing, and VRM fallback logs a clear reason in addition to missing-model/load-failure reasons. Documentation and example-only env files cover the 8001 backend + 5175 frontend profile without committing real `.env` files. | Pass |
| Fixed frontend dev port | RED/GREEN targeted test `npm.cmd run test:run --prefix frontend -- devServerConfig.test.ts`; occupied-port runtime check `npm.cmd run dev --prefix frontend -- --host 127.0.0.1 --port 5175 --strictPort`; HTTP check `Invoke-WebRequest http://127.0.0.1:5175 -UseBasicParsing`; full frontend suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend`; backend suite `python -m pytest backend/tests -v` | Regression test first failed because `vite.config.ts` had no `server` config and `package.json` used `vite --host 0.0.0.0`. Green test now proves Vite is pinned to `host=127.0.0.1`, `port=5175`, and `strictPort=true`, and the package script no longer overrides the fixed config. Current port check showed 5175 occupied by `node.exe` PID 18964; the strict dev command failed clearly with `Error: Port 5175 is already in use` and did not switch to 5178. Existing listener returned HTTP 200 at `http://127.0.0.1:5175`. | Pass |
| Cinematic AI subtitle mode | `npm.cmd run test:run --prefix frontend -- AiSubtitle.test.tsx useSubtitlePlayback.test.ts App.integration.test.tsx`; full suite `npm.cmd run test:run --prefix frontend` | Targeted tests passed: subtitle helper splits natural phrases, preserves one current phrase at a time, progressive speaking subtitles advance by cue timing, idle/thinking/error/escalation states show short customer-facing copy, the customer transcript is hidden from the main kiosk UI, the full AI response is not duplicated in the voice feedback area, and the subtitle region remains aria-live; full frontend suite passed with 16 files and 80 tests | Pass |
| Controlled UI subtitles and leaflet actions | `python -m pytest backend/tests/test_ai_brain.py backend/tests/test_api.py backend/tests/test_services.py -v`; `npm.cmd run test:run --prefix frontend -- App.integration.test.tsx useVoiceInteraction.test.ts` | Backend verifies whitelisted `ui_actions`, product-specific promotion leaflet, confirmation-open modal flow, general promotion/campaign galleries, active branch-valid leaflet filtering, pharmacist confirmation, and red-flag safety priority; frontend verifies AI subtitle mode, whitelisted action execution, ignored unknown action, leaflet modal, no-product promotion/campaign choices, campaign gallery, and pharmacist override | Pass |
| Floating hologram leaflet card | RED targeted test `npm.cmd run test:run --prefix frontend -- LeafletModal.test.tsx App.integration.test.tsx typedInputLayout.test.ts` failed with 8 expected failures while the enlarged leaflet viewer still used the old dark `.leaflet-modal` container, top header/banner/headings, visible `Holographic leaflet stage` copy, visible `Swipe to browse`, and old full-stage labels; GREEN targeted test passed with 3 files and 46 tests | Leaflet viewer now renders as a transparent floating hologram card over the existing kiosk UI. The leaflet image is the hero, neighboring leaflets remain subtle depth previews, the whole floating stage handles drag/touch/mouse/trackpad wheel, outside click closes, inside stage/metadata clicks do not close, Escape and keyboard arrows work from the open viewer even when focus is not inside the stage, visible X/Previous/Next/dots/`1 / 3` indicators remain absent, top title/header and visible swipe hint are removed, and no particles/fog/smoke/dust/light-trail/heavy modal background effects are added. Browser QA on `http://127.0.0.1:5175` confirmed the floating card DOM/styles, drag navigation, inside-click retention, outside-click close, and Escape close. Full suite/build/backend verification is recorded in the standard rows after this entry. | Pass |
| Optional Three.js and VRM avatar renderers | `npm.cmd run test:run --prefix frontend -- src/components/avatar/AvatarRenderer.test.ts src/components/avatar/AvatarModel.test.ts src/components/AvatarAssistant.test.tsx src/components/avatar/VrmAvatarRenderer.test.tsx src/hooks/useAvatarIdleMotion.test.ts src/hooks/useAvatarLipSync.test.ts` | Covered in full frontend suite; Lottie default, Three.js renderer state accessibility, VRM renderer state accessibility, Vite-exposed `VITE_VRM_MODEL` config selection, GLB/VRM model URL resolution, alternate `vita-new` model key, VRM-backed marker, fallback marker, expression mapping, reduced-motion idle stability, and amplitude-based mouth movement covered | Pass |
| VRM assistant state behavior mapping | `npm.cmd run test:run --prefix frontend -- src/components/avatar/VrmAvatarRenderer.test.tsx src/hooks/useAvatarLipSync.test.ts src/hooks/useAvatarIdleMotion.test.ts src/components/AvatarAssistant.test.tsx src/App.integration.test.tsx`; full suite `npm.cmd run test:run --prefix frontend`; build `npm.cmd run build --prefix frontend` | Targeted tests prove VRM state contract for idle/listening/thinking/speaking/error/pharmacist escalation, customer-facing labels `Ready`, `Listening`, `Thinking`, `Speaking`, `Try Again`, and `Pharmacist Requested`, speaking-only audio-gated mouth movement, closed mouth for idle/listening/thinking/error/escalation, serious non-smiling pharmacist expression, thinking scan marker, safety/warning glow markers, and no technical VRM labels in the customer UI; full frontend suite passed with 16 files and 90 tests and production build completed | Pass |
| GLB humanoid avatar asset | Review `frontend/src/assets/avatar/vitakiosk-avatar.glb` and `docs/avatar-model.md` | Lightweight fictional GLB placeholder exists at the reviewed path; docs explain self-hosted model replacement, licensing, performance, no-runtime-service, and fallback constraints | Pass |
| VRM self-hosted avatar asset | Review `frontend/src/assets/avatar/vita.vrm`, `docs/avatar-model.md`, and `spec/04-ai-avatar-spec.md` | User-provided local VRM demo model exists at the reviewed self-hosted path; renderer supports `VITE_AVATAR_RENDERER=vrm`, idle motion, blinking, expression mapping, amplitude lip sync, and safe fallback; model is about 15.37 MB and should be optimized before production use when practical | Pass |
| Alternate VRM avatar model selection | In-app Browser at 1024x768 with `VITE_AVATAR_RENDERER=vrm` and `VITE_VRM_MODEL=vita-new`; screenshot review `reports/evidence/vrm-avatar-vita-new-1024x768.png`; comparison screenshot `reports/evidence/vrm-avatar-vita-original-1024x768.png` | Runtime DOM reported `data-avatar-renderer="vrm"`, `data-avatar-model-key="vita-new"`, `data-avatar-model-url="/src/assets/avatar/vita-new.vrm"`, WebGL `available`, `canvasCount=1`, and `lottieCount=0`; the old `vita.vrm` remained available and separately reported `data-avatar-model-key="vita"`; `vita-new.vrm` is a distinct 16.53 MB file with SHA256 `6A6AAB76BB4F0B6DEA36A917EE233D936CCA6E7D358465898F294161E13C263F` | Pass |
| Restored original VRM avatar lighting QA | In-app Browser at 1024x768 with `VITE_AVATAR_RENDERER=vrm` and `VITE_VRM_MODEL=vita-new`; screenshot review `reports/evidence/vrm-avatar-original-lighting-restored-1024x768.png` | Runtime DOM reported `data-avatar-renderer="vrm"`, `data-avatar-model-key="vita-new"`, `data-avatar-framing="full-body"`, `data-avatar-stage="full-body-chamber"`, `data-avatar-orbit-layer="background"`, WebGL `available`, `canvasCount=1`, and no temporary natural-lighting override markers; screenshot evidence shows the original brighter VRM lighting style restored while full-body framing and dark neon layout remain unchanged | Pass |
| VRM pose and bust portrait runtime QA | In-app Browser at 1024x768 with `VITE_AVATAR_RENDERER=vrm`; screenshot review `reports/evidence/vrm-avatar-bust-portrait-1024x768.png` | Runtime evidence shows self-hosted VRM model active in a vertical portrait stage, no circular hologram mask, face/head/shoulders/upper chest visible, no raised hands, and no improvised hand/lower-arm gesture override visible | Pass |
| VRM full-body chamber runtime QA | In-app Browser at 1024x768 and 1366x768 with `VITE_AVATAR_RENDERER=vrm`; screenshot review `reports/evidence/vrm-avatar-full-body-1024x768.png` and `reports/evidence/vrm-avatar-full-body-1366x768.png` | Runtime evidence reports `data-avatar-framing="full-body"`, `data-avatar-stage="full-body-chamber"`, no horizontal overflow, no document scrolling in landscape, the head is not cropped, the avatar is larger in the display chamber, and the primary Tap to Speak control remains visible | Pass |
| VRM background orbit runtime QA | In-app Browser at 1024x768, 1366x768, and 1920x1080 with `VITE_AVATAR_RENDERER=vrm`; screenshot review `reports/evidence/vrm-avatar-background-orbit-1024x768.png` and `reports/evidence/vrm-avatar-background-orbit-1366x768.png` | Runtime evidence reports `data-avatar-orbit-layer="background"`, no horizontal overflow, no document scrolling in landscape, the orbit reads as a larger background halo/portal, and no visible foreground ring crosses the avatar face, torso, arms, hands, or dress | Pass |
| Responsive kiosk runtime QA | In-app Browser with `VITE_AVATAR_RENDERER=vrm` at 1024x768, 1280x720, 1366x768, 1440x900, 1920x1080, and 768x1024 | Required landscape viewports reported no horizontal overflow and no document scrolling; iPad portrait reported no horizontal overflow with vertical scrolling fallback; VRM portrait panel, Tap to Speak, poster, shelf map, ERP panel, and pharmacist safety panel remained visible/readable | Pass |
| Promotion leaflet containment and overlay QA | `npm.cmd run test:run --prefix frontend -- PromotionPoster LeafletModal typedInputLayout`; headless Chrome/CDP measurement at 1024x768, 1366x768, 1920x1080, plus enlarged overlay at 1024x768 and 1366x768 | Collapsed cards render up to 3 active leaflets but CSS shows 1 visible card at 1024x768 and 1366x768, 2 side-by-side cards at 1920x1080, no horizontal overflow, no visible collapsed metadata, `object-fit: contain`, and each visible image remains inside its frame. Enlarged viewer is a body portal with `position: fixed`, `z-index: 900`, `isolation: isolate`, `perspective: none`, hidden neighbor previews, active card as the top element at its center point, no metadata overlap, and click-outside closes. Screenshots: `reports/evidence/leaflet-collapsed-1024x768-fixed.png`, `reports/evidence/leaflet-collapsed-1366x768-fixed.png`, `reports/evidence/leaflet-collapsed-1920x1080-fixed.png`, `reports/evidence/leaflet-overlay-1024x768-foreground.png`, `reports/evidence/leaflet-overlay-1366x768-foreground.png` | Pass |
| Three.js avatar runtime QA | `$env:VITE_AVATAR_RENDERER='threejs'; npm.cmd run dev --prefix frontend -- --host 127.0.0.1 --port 5174`; in-app Browser at 1024x768 | Runtime DOM reported `data-avatar-renderer="threejs"`, WebGL `available`, `canvasCount=1`, `lottieCount=0`, viewport/document 1024x768; screenshot saved to `reports/evidence/threejs-avatar-renderer-1024x768.png` | Pass |
| Shelf map component | `npm.cmd run test:run --prefix frontend -- src/components/ShelfMap.test.tsx` | 2 tests passed: route landmarks and unavailable-location non-invention | Pass |
| Frontend build | `npm.cmd run build --prefix frontend` | TypeScript and Vite production build completed; default bundle 368.96kB JS before gzip, optional lazy ThreeAvatarRenderer chunk 80.25kB, VrmAvatarRenderer chunk 195.23kB, AvatarModel chunk 862.17kB, CSS bundle 84.95kB, local VRM asset 15,369.46kB, and alternate local VRM asset 16,532.72kB | Pass |
| Dependency audit | `npm.cmd audit --prefix frontend --audit-level=moderate` | 0 vulnerabilities | Pass |
| Repository contract | `node scripts/check-repository.mjs` | Required structure and secret placeholders verified | Pass |
| Spec coverage | `node scripts/check-specs.mjs` | 13 feature specs passed coverage check | Pass |
| Staged-file safety | `node scripts/check-staged-files.mjs` | Run before every task commit; no prohibited staged path | Pass |
| Tracked-file safety | `node scripts/check-staged-files.mjs --tracked` | All tracked paths checked; no prohibited business or secret file | Pass |
| Protected-path declaration | Review `AGENTS.md`, README, architecture, and spec 07 | Protected ERP release directory is explicitly out of scope and was not accessed | Pass |
| Dark neon iPad landscape QA | In-app Browser at 1024×768 | Viewport 1024×768; document 1024×768; dark gradient body background; 9 accessible regions; console errors/warnings: 0 | Pass |
| Refined premium kiosk visual QA | In-app Browser at 1024x768 | Viewport 1024x768; document 1024x768; no light dashboard panels; holographic avatar, poster campaign display, stronger shelf route map, compact ERP panel, and safety control panel verified; console errors/warnings: 0 | Pass |
| Tap voice control QA | Click primary `Tap to Speak` | Ready label present; click entered permission-error path because microphone permission was not granted; `Try Again` and accessible error feedback verified; real listening/Tap to Stop transition not manually verified | Permission boundary |
| Silence auto-stop and Start reset | `npm.cmd run test:run --prefix frontend -- src/hooks/useVoiceInteraction.test.ts src/App.integration.test.tsx`; In-app Browser at `http://127.0.0.1:5175` 1024x768; screenshot `reports/evidence/voice-start-reset-5175-1024x768.png` | Tests prove `MIN_RECORDING_MS`, `SILENCE_STOP_MS`, and `SILENCE_RMS_THRESHOLD` drive Web Audio analyser auto-stop into the normal thinking/speaking/idle flow; browser QA showed `Tap to Speak`, `Start`, no `Hold to Speak`, and `Start` returned the kiosk to ready without refresh | Pass |
| Local 5175 CORS | `python -m pytest backend/tests/test_health.py -v`; runtime `OPTIONS /health` with `Origin: http://127.0.0.1:5175` | Tests cover `localhost` and `127.0.0.1` on ports 5173 and 5175; refreshed local backend returned status 200 and `Access-Control-Allow-Origin: http://127.0.0.1:5175` | Pass |
| Cinematic kiosk screenshot | In-app Browser `tab.screenshot({ fullPage: false })` | Dark AI bay, primary Tap to Speak, poster-style promotion, shelf map route, ERP provenance, and pharmacist safety panel visible | Pass |
| Shelf map screenshot | In-app Browser `tab.screenshot({ fullPage: false })` | Cyan route, purple target marker, current-position marker, aisle blocks, and Aisle/Shelf/Level details are visible | Pass |
| Narrow responsive QA | In-app Browser at 390×844 | 7 regions; document width 375px within viewport; no horizontal overflow; no console errors/warnings | Pass |
| Pharmacist escalation reset QA | In-app Browser at 1024x768 on `http://127.0.0.1:5173`; click `Request assistance`, then `Start New Customer`; screenshots `reports/evidence/pharmacist-escalation-confirmation-1024x768.png` and `reports/evidence/pharmacist-escalation-reset-ready-1024x768.png` | Confirmation showed `Pharmacist assistance requested`, `A pharmacist has been notified`, escalation ID `ESC-0051`, and one `Start New Customer` button; clicking reset returned the kiosk to ready state with enabled `Tap to Speak`, normal pharmacist assistance panel, no confirmation text, and no browser refresh | Pass |
| Microphone interaction QA | Browser permission and hold/release | Automated MediaRecorder boundary is covered; physical microphone permission was not granted | Not manually verified |
| VitaKiosk Asia site responsive QA | `npm.cmd run site:test`; in-app Browser at 375x667, 390x844, 414x896, 430x932, 768x1024, 820x1180, 1024x768, 1024x1366, 1366x768, 1440x900, 1536x864, 1920x1080, and 2560x1440 across `/`, plus key route pages at mobile/tablet/desktop sizes | 67 route/viewport checks reported zero horizontal overflow, zero offscreen actionable elements, zero small visible touch targets, no placeholder route copy, no console warnings/errors; phone widths use `compact-deck` media orbit, tablet/desktop use contained `cylindrical-orbit`; mobile fullscreen demo portal hit-tested the `Scan Product` button and opened the simulated `scan_product` overlay | Pass |
| VitaKiosk Asia assistant avatar asset | `npm.cmd run site:test`; `npm.cmd run site:build`; in-app Browser on `http://127.0.0.1:5176/vitakiosk?avatar=approved` | The embedded interactive demo renders `/assets/avatar/ai-pharmacy-assistant-avatar.png` as `.mini-avatar-image` with natural size 306x631, removes the old CSS-drawn avatar figure, keeps `Tap to Speak` interaction working, reports zero horizontal overflow, and shows no console warnings/errors | Pass |

Automated evidence uses fictional mock data and no live provider credential.

## Visual fidelity ledger

| Comparison point | Concept evidence | Render evidence | Result |
|---|---|---|---|
| Layout | Approved B direction uses left AI bay, center conversation/product/map deck, and right retail/safety rail | Render fits 1024×768 exactly with no document scroll | Matched |
| Palette | Approved direction requires dark navy/black, cyan route/glow, purple AI/promo accents, glass panels | Computed body background is dark gradient; screenshot has no white dashboard surface | Matched |
| Voice control | Main interaction must be `Tap to Speak`; secondary action must reset with `Start` / `Start New Customer` instead of a small Hold fallback | Primary button is `Tap to Speak`; `Start` is the secondary reset action; `Hold to Speak` is no longer shown in the kiosk UI | Matched |
| Avatar | Current direction requires a futuristic AI assistant screen with a larger full-body/near-full-body VRM display chamber and background-only orbit halo | Render uses a self-hosted VRM avatar in a dark holographic chamber with head, arms, and lower body visible; orbit/halo is moved behind the character, with waveform and state label remaining foreground UI | Matched |
| Promotion and campaigns | Promotion/campaign area must look like a poster/gallery and only show active branch-aware mock leaflet data | Poster frame, galleries, modal, and no-product choices use active SG-001 mock leaflets, mock VitaFlow provenance, validity, and no invented discount or campaign claim | Matched |
| Shelf navigation | Required as an indoor pharmacy map with current marker, target marker, route, aisle/shelf/level | Screenshot shows Entrance, Aisle 03, Shelf A-03, Level 02, cyan route, purple target | Matched |
| ERP and safety | ERP must be small floating system panel; pharmacist panel must be safety/escalation oriented | Render shows Mock VitaFlow, SG-001, Mock mode, no customer data, and AI-not-pharmacist safety copy | Matched |
| Data and safety copy | VitaFlow/mock data is source of truth; no diagnosis, no customer data, no invented stock/price/location | Render uses existing mock product, price, stock, shelf, branch, promotion, and safety messaging only | Matched |
| Responsive scaling | Kiosk should adapt across iPad landscape, laptop, desktop, and narrow tablet without horizontal overflow | Browser measurements and screenshots cover 1024x768, 1280x720, 1366x768, 1440x900, 1920x1080, and 768x1024; landscape viewports fit without document scrolling, portrait stacks with vertical scroll only | Matched |
| Voice permission | Browser microphone permission was not granted during QA | Error path and `Try Again` state verified; real listening path remains covered by code contract and automated MediaRecorder tests only | Intentional evidence limitation |
| Site mini app voice action | Tap to Speak should behave like a compact action button, not a stretched empty panel | Browser QA on `/vitakiosk?avatar=approved` measured `.mini-tap-button` at 360x86 with no horizontal overflow; clicking it entered `listening` and showed `Where is Relief Balm?` | Matched |
| Site mini app logical medium layout | Tablet/narrow desktop layout should keep assistant controls together and avoid a stranded voice button in empty space | Browser QA on `/vitakiosk?avatar=approved` at 912x1270 showed avatar, Tap to Speak, and language chips stacked in the left assistant column; subtitle, product, shelf, input, and support panels fill the right/bottom areas with no horizontal overflow; clicking Tap to Speak entered `listening` | Matched |

Concept: `docs/superpowers/specs/2026-06-21-dark-neon-cinematic-kiosk-design.md` and the approved visual companion B direction.

Shelf map acceptance screenshot: [iPad landscape route map](evidence/shelf-map-ipad-landscape.png).

Dark neon kiosk acceptance screenshot: [1024 × 768 Cinematic AI Bay](evidence/dark-neon-kiosk-ipad-landscape.png).

Refined premium kiosk screenshot: [1024x768 dark neon visual polish](evidence/dark-neon-kiosk-refined-1024x768.png).

VRM bust portrait screenshot: [1024x768 VRM assistant bust portrait](evidence/vrm-avatar-bust-portrait-1024x768.png).

VRM full-body chamber screenshots: [1024x768](evidence/vrm-avatar-full-body-1024x768.png), [1366x768](evidence/vrm-avatar-full-body-1366x768.png).

VRM background orbit screenshots: [1024x768](evidence/vrm-avatar-background-orbit-1024x768.png), [1366x768](evidence/vrm-avatar-background-orbit-1366x768.png).

Alternate VRM model screenshots: [vita-new 1024x768](evidence/vrm-avatar-vita-new-1024x768.png), [vita original 1024x768](evidence/vrm-avatar-vita-original-1024x768.png).

Restored original VRM avatar lighting screenshot: [1024x768](evidence/vrm-avatar-original-lighting-restored-1024x768.png).

Responsive kiosk screenshots: [1024x768](evidence/responsive-kiosk-1024x768.png), [1366x768](evidence/responsive-kiosk-1366x768.png), [1920x1080](evidence/responsive-kiosk-1920x1080.png).

Promotion leaflet containment screenshots: [1024x768 collapsed](evidence/leaflet-collapsed-1024x768-fixed.png), [1366x768 collapsed](evidence/leaflet-collapsed-1366x768-fixed.png), [1920x1080 collapsed side-by-side](evidence/leaflet-collapsed-1920x1080-fixed.png), [1024x768 foreground overlay](evidence/leaflet-overlay-1024x768-foreground.png), [1366x768 foreground overlay](evidence/leaflet-overlay-1366x768-foreground.png).

Measured subtitle/Product overlap screenshots: [1024x768](evidence/subtitle-product-overlap-1024x768.png), [1366x768](evidence/subtitle-product-overlap-1366x768.png).

Product panel clipping screenshots: [1024x768](evidence/product-panel-no-clipping-1024x768.png), [900x768](evidence/product-panel-no-clipping-900x768.png), [1366x768](evidence/product-panel-no-clipping-1366x768.png).

Shelf Navigation route clipping screenshots: [1024x768](evidence/shelf-route-no-clipping-1024x768.png), [900x768](evidence/shelf-route-no-clipping-900x768.png), [1366x768](evidence/shelf-route-no-clipping-1366x768.png).

Pharmacist escalation reset screenshots: [confirmation](evidence/pharmacist-escalation-confirmation-1024x768.png), [ready after reset](evidence/pharmacist-escalation-reset-ready-1024x768.png).

Voice Start reset screenshot: [5175 1024x768](evidence/voice-start-reset-5175-1024x768.png).

Accessibility typed-input screenshots: [typed input panel 1024x768](evidence/typed-input-area-1024x768.png), [popup typing screen 1024x768](evidence/typed-input-popup-keyboard-1024x768.png), [native keyboard mode 1024x768](evidence/typed-input-native-mode-1024x768.png). New screenshot evidence for the native-first popup redesign should replace the older Chinese quick-input evidence after browser capture.

The committed screenshots are limited to mock data and contain no real customer, sales, database, log, backup, token, password, or ERP release data.
