# VitaKiosk Avatar Assets

`vitakiosk-avatar.glb` is a lightweight fictional humanoid placeholder for the optional Three.js renderer.

Replace it with a reviewed Ready Player Me `.glb` only after checking iPad performance, file size, privacy, and safety constraints. Lottie remains the default renderer, and Three.js must remain optional via `VITE_AVATAR_RENDERER=threejs`.
