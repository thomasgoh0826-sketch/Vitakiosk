# Services

Provider-neutral Python contracts and mock adapters for VitaFlow, voice, AI, promotions, posters, product vision, and safety.

`providers.py` builds the active adapter bundle from explicit provider selectors. Defaults stay mock; live-provider placeholders are only selected through local `.env` and do not make network calls in this demo.
